"""Shared layer primitives owned by timme.

Currently empty — timme reuses ``timm.layers`` for activations, norms,
patch embeds, attention pools, etc. As we evolve, layers that need to
diverge from timm's behavior (or new ones not present in timm) will land
here.
"""
